package com.vyra.app.core.media.model

/**
 * High-level media category supported by the VYRA media pipeline.
 */
enum class MediaType {
    IMAGE,
    VIDEO
}

/**
 * Origin source of the acquired media.
 */
enum class MediaSource {
    CAMERA,
    GALLERY
}
